# 同业对比看板 · 更新流程（每期更新时按此执行）

数据源（都在本目录，可能同时有或只有其一）：
- 「同业截至MMDD.png」微信长图（"3年前买的私募"排名表）
- 「附件3：私募产品表现表-YYYYMMDD.xlsx」信托产品周度表现表
看板两处副本，**必须都更新**：
1. 项目源文件：`同业对比看板\index.html`（有 git 仓库，每期 commit）
2. Widget 展示副本：`E:\daimon-share\daimon\agents\main\blueprint\widgets\widget_716b31c7-6d25-445f-b25d-a5ae5fff9707\workspace\index.html`（Widget「维纳1号 · 同业对比看板」，更新后用 Widget.update 改描述 + Widget.show 重新挂载）

## 步骤

1. **读图转录**：长图切成约 1700px 高的分段逐段读取（ReadMediaFile），疑点用原图 region 裁剪核实。转录为 (板块, 名称, 管理人, y3, y2, y1, ytd, y25, y24, y23)。
2. **收录口径**（与 2026-08-28 期一致）：只收录 12 个股票类板块——300/A500/500/1000/2000/小市值/红利/全指指增、量化选股、量化择时、主观多头、股票多空。市场中性、宏观、CTA、债券、套利不收录。
3. **产品池规则**：
   - **附件3 xlsx（若有新期）**：用主表「净值-MMDD更新」（表头在第 2 行）整表重建 xlsx 产品（无 src 字段），替换上期全部 xlsx 产品。字段映射：n=产品名称、st=产品策略、inc=成立时间、mg=管理机构简称、nav=最新净值，w1/m1/y1/ytd/dd/ann/y25/y24/y23 = 对应增幅列 ×100 保留 2 位（'--'/'#REF!'→null）；tp 按分类 sheet（对冲类→私募对冲、股票（量化/主观）→私募股票、债券类→私募债券、境外产品→境外产品、多元专户→其他私募）判定，名称做去横线空格归一化匹配，匹配不上再按 st 推断。参考 `_tmp_merge0828.py`。
   - **长图**：上期长图产品中不在新图里的删除；图中有而看板没有的作为新增（tp="私募股票"，inc/nav/w1/m1/dd/ann=null，src="微信长图"）；注意「此前仅信托 xlsx 收录、长图也有」的产品要补入长图口径。st 映射见 `_tmp_transcribed.py` 的 ST_MAP。
   - 两类产品并存（同 08-21/08-28 期），按名称精确匹配去重核对，不匹配的不静默丢。
   - 若某期只有附件3没有新长图：长图产品原样保留，只重建 xlsx 部分，asOf 取附件3净值日期；只有长图没有新附件3 时，xlsx 产品删除。
4. **维纳1号**：从 `E:\工作\06_净值数据\源数据（总）.xlsx`（先复制进本目录）「最大回撤计算」表重算 nav/w1/m1/y1/ytd/dd/ann，截止日期与长图一致；y25/y24/y23 静态沿用。
5. **指数**：沪深300、创业板指取自源数据 xlsx；中证500/1000/全指用 mcp__txdl-equity__query_index 取日收盘；当年最大回撤从各自年内序列算。
6. **构建**：参考 `_tmp_build.py` + `_tmp_transcribed.py`（2026-08-28 期留存模板）。匹配按产品名精确匹配，未匹配的打印出来人工核对，不要静默丢；注意「仅有信托 xlsx 口径、本期需补入长图口径」的产品要作为新增收录。
7. **写入**：替换 `同业对比看板\index.html` 第 129 行 `const DATA = ...;`，同步更新头部硬编码的 `截至日期 · 产品数 · 来源` 和图例里的 `净值 X.XXXX`；数据存档到 `同业过往数据\同业对比_数据_YYYYMMDD.json`。
8. **验证**：本地起临时 http 服务器 + InAppBrowser 打开验证（KPI 卡、明细表行数、分策略中位数图），验证完关闭服务器。
9. **收尾**：`同业对比看板` 下 git commit（格式：`同业对比看板 YYYY-MM-DD期`）**并 `git push origin main`**——远程是 GitHub Pages 网页版 https://harrysong1118.github.io/fund-dashboard/ ，推送约 1 分钟后用 curl 带时间戳验证线上头部日期。清理 `_tmp_slice_*` 等临时文件；**同步 Widget 副本**（见上）。

## 三处副本清单（每次更新都必须全部生效）

1. 项目源文件：`同业对比看板\index.html`（git 本地提交）
2. Widget 展示副本：`E:\daimon-share\daimon\agents\main\blueprint\widgets\widget_716b31c7-6d25-445f-b25d-a5ae5fff9707\workspace\index.html`
3. 网页版：https://harrysong1118.github.io/fund-dashboard/ （由第 1 处的 git push 发布）

## 一键更新按钮（2026-09-06 起）

看板右上角「更新同业数据」按钮 → 触发后台任务「同业对比看板 · 按钮触发更新」（automation_37e7c130-25c3-4c07-af31-2c3680ef06c6），自动执行上述全部流程并回显结果到按钮旁。该任务的提示词中同样必须包含 git push 步骤。
